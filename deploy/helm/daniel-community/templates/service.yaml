apiVersion: v1
kind: Service
metadata:
  name: {{ include "daniel-community.fullname" . }}
  labels:
    {{- include "daniel-community.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "daniel-community.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http